<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GoogleCalendarManagement.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="20"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1">
            <StackPanel Spacing="16">
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="Calendar" />

                <StackPanel
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button x:Name="YearButton" Click="YearButton_Click" Content="Year" />
                    <Button x:Name="MonthButton" Click="MonthButton_Click" Content="Month" />
                    <Button x:Name="WeekButton" Click="WeekButton_Click" Content="Week" />
                    <Button x:Name="DayButton" Click="DayButton_Click" Content="Day" />
                    <Button Content="Previous" Command="{x:Bind ViewModel.NavigatePreviousCommand}" />
                    <Button Content="Today" Command="{x:Bind ViewModel.NavigateTodayCommand}" />
                    <Button Content="Next" Command="{x:Bind ViewModel.NavigateNextCommand}" />
                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="{x:Bind ViewModel.BreadcrumbLabel, Mode=OneWay}" />
                    <CalendarDatePicker
                        x:Name="JumpToDatePicker"
                        Header="Jump to date"
                        DateChanged="JumpToDatePicker_DateChanged" />
                    <Button x:Name="SettingsButton" Click="SettingsButton_Click" Content="Settings" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Frame x:Name="CalendarFrame" />

            <Border
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="24"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="16"
                Visibility="{x:Bind ViewModel.EmptyStateVisibility, Mode=OneWay}">
                <TextBlock
                    MaxWidth="420"
                    Text="No events synced yet. Go to Settings → Connect Google Calendar to sync."
                    TextAlignment="Center"
                    TextWrapping="WrapWholeWords" />
            </Border>

            <ProgressRing
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
        </Grid>
    </Grid>
</Page>
