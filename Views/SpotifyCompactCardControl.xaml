<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoogleCalendarManagement.Views.SpotifyCompactCardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <StackPanel
            Spacing="2"
            Visibility="{Binding DataVisibility}">
            <TextBlock
                Opacity="0.76"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{Binding ListeningTimeLabel}"
                TextWrapping="NoWrap" />
            <TextBlock
                Opacity="0.76"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{Binding TrackCountLabel}"
                TextWrapping="NoWrap" />
        </StackPanel>

        <TextBlock
            Opacity="0.54"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="No Spotify data"
            TextWrapping="WrapWholeWords"
            Visibility="{Binding NoDataVisibility}" />
    </Grid>
</UserControl>
